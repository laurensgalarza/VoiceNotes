import React, {useState} from "react";
import { X } from 'lucide-react';

export default function UploadForm() {
    //for story gen, files need to be put into an array of strings
    return (
        <div>

        </div>
    )

}